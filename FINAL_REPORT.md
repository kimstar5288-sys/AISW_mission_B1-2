# B1-2 평가 제출용 설명

## 1. 결과물 개요

React를 사용해 새로고침 없이 화면이 전환되는 SPA를 구현했다. 상단의 HOME, WORK, ABOUT 버튼을 누르면 `page` 상태가 변경되고, 상태에 해당하는 컴포넌트가 다시 렌더링된다. 화면 전환에는 CSS animation을 적용해 아래에서 위로 올라오며 흐려졌다가 선명해지는 효과를 넣었다.

## 2. 상태는 어디에 두었는가?

여러 화면에서 함께 사용하는 상태는 최상위 `App`에 두었다.

- `page`: 현재 어떤 화면을 보여줄지 결정한다.
- `dark`: 다크/라이트 테마를 결정한다.

특정 컴포넌트 내부에서만 사용하는 상태는 해당 컴포넌트에 두었다.

- `Work`의 `activeFilter`: 프로젝트 필터에만 필요하다.
- `About`의 `messageVisible`: 안내 메시지 표시 여부에만 필요하다.

이렇게 하면 불필요하게 모든 상태를 App에 몰아넣지 않고, 필요한 위치에 가깝게 상태를 배치할 수 있다.

## 3. 언제 리렌더링되는가?

React의 `useState`로 만든 상태가 `set...` 함수에 의해 바뀌면 해당 상태를 사용하는 컴포넌트가 다시 렌더링된다. 예를 들어 HOME에서 VIEW WORK 버튼을 누르면 `setPage('work')`가 실행되고, App은 `page === 'work'`에 해당하는 Work 컴포넌트를 화면에 그린다.

## 4. 이벤트 → 상태 변경 → UI 업데이트 흐름

예: 상단 WORK 버튼 클릭

1. 사용자가 WORK 버튼을 클릭한다.
2. `onClick={() => onNavigate('work')}`가 실행된다.
3. `handleNavigate`가 `setPage('work')`를 호출한다.
4. `page` 상태가 `home`에서 `work`로 바뀐다.
5. App이 다시 렌더링된다.
6. Work 컴포넌트가 나타난다.
7. `screen-transition` 애니메이션이 실행되어 화면이 자연스럽게 바뀐다.

이 과정이 React의 기본적인 상태-렌더링 흐름이다.

## 5. SPA로 만든 이유

SPA는 한 번 불러온 페이지 안에서 필요한 UI만 변경하기 때문에 메뉴를 누를 때마다 새로운 HTML 문서를 다시 받아올 필요가 없다. 따라서 화면 전환이 빠르고, 앱처럼 자연스러운 사용자 경험을 만들 수 있다.

## 6. 컴포넌트 분리 기준

- `Header`: 모든 화면에서 공통으로 보이는 메뉴와 테마 버튼
- `Home`: 첫 화면의 소개와 CTA
- `Work`: 프로젝트 목록과 필터 기능
- `About`: 미션 설명과 메시지 토글 기능
- `App`: 공통 상태와 컴포넌트 연결

화면의 역할과 상태의 책임을 기준으로 컴포넌트를 나눴다.

## 7. 미션에서 확인할 수 있는 React 핵심 개념

- 컴포넌트 기반 UI 구성
- `useState`를 이용한 상태 관리
- props로 이벤트 함수 전달
- 이벤트 핸들링
- 조건부 렌더링
- 배열 `map`, `filter`
- `useMemo`를 이용한 필터 결과 계산
- 선언적 UI 업데이트
- CSS 애니메이션을 통한 자연스러운 전환
- 반응형 디자인

## 8. 최종 설명 한 문장

“사용자가 버튼을 누르면 이벤트가 발생하고, 이벤트가 React 상태를 바꾸며, 바뀐 상태에 따라 필요한 컴포넌트가 다시 렌더링되고 CSS 애니메이션이 적용되어 화면이 자연스럽게 전환되는 SPA를 만들었습니다.”
